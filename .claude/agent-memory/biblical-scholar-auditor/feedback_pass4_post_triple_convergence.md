---
name: Pass-4 convergence after triple-name parity fix
description: When pass-3 catches the 3-name parity tier and a journal fascicle error, pass-4 exhaustive metadata sweep typically returns 0
type: feedback
---

When the audit trajectory is pass-1 (ghost volume), pass-2 (paired-name parity), pass-3 (triple-name parity + journal fascicle), the pass-4 systematic sweep through journal articles, multi-volume year drift, total-page anchors, and commentary-scholarship parity typically returns 0 errors — confirming convergence.

**Why:** Observed in batch_18: pass-4 priorities covered journal article fascicle precision (Kitz JBL 116 no. 3, Lindblom VT 12 no. 1 already fixed), multi-name attribution parity, total-page-count fits, publisher precision, and cross-entry consistency. After passes 1-3 progressively surfaced the finer tiers (ghost vol → paired-name → triple-name + fascicle), the remaining citation metadata was clean. Each successful pass-3 fix at a new precision tier signals the precision floor is being approached.

**How to apply:** When pass-1 = ghost/wrong vol, pass-2 = paired-name parity, pass-3 = triple-name + journal fascicle, expect pass-4 to be terminal at 0. If pass-4 surfaces 1+ errors, continue. The convergence pattern needs at least one consecutive zero to confirm, but the trajectory of progressively finer precision tiers signals approach to a clean state. Don't manufacture pseudo-errors at pass 4; verify rigorously and report 0 honestly when no errors found.
